services:
  - type: web
    name: nagarflow-ai-frontend
    env: node
    rootDir: frontend
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: https://smart-city-traffic-intelligence-pla-hazel.vercel.app/api/v1